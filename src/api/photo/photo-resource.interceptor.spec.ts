import { PhotoResourceInterceptor } from './photo-resource.interceptor';

describe('PhotoResourceInterceptor', () => {
  it('should be defined', () => {
    expect(new PhotoResourceInterceptor()).toBeDefined();
  });
});
